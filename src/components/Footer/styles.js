import styled from 'styled-components'

export const ContainerFooter = styled.footer`
	color: #ffffffdd;
	background: #722787;

	h2 {
		font-size: 1rem;
		font-weight: normal;
	}

	a {
		font-size: 0.85rem;
		color: #ffffffaa;
		text-decoration: none;
		display: flex;
		align-items: center;
		margin-top: 6px;

		i,
		svg {
			margin-right: 6px;
		}
	}
`
